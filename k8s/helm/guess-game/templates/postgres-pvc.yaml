apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "guess-game.fullname" . }}-postgres-pvc
  labels:
    {{- include "guess-game.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.postgres.storage }}
